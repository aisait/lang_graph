FROM python:3.11-slim

WORKDIR /app

RUN apt-get update && apt-get install -y --no-install-recommends \
    gcc libpq-dev && \
    rm -rf /var/lib/apt/lists/*

# Instalación de dependencias con versiones compatibles
RUN pip install --no-cache-dir --upgrade pip && \
    pip install --no-cache-dir \
        "langgraph-cli[inmem]>=0.4.31" \
        langchain-core==0.2.43 \
        langchain==0.2.17 \
        langgraph==0.2.56 \
        langchain-openai==0.1.25 \
        langchain-community==0.2.19 \
        "langsmith<0.2.0" \
        langgraph-checkpoint==2.0.7 \
        langgraph-checkpoint-postgres==2.0.7 \
        openai>=1.50.0

COPY . .

ENV PORT=8080
EXPOSE 8080

# COMANDO CORRECTO: langgraph dev (no requiere Docker)
CMD ["sh", "-c", "langgraph dev --host 0.0.0.0 --port ${PORT:-8080} --no-browser"]
