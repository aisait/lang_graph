# requirements.txt (backend)
# Dependencias del backend JARVI 2.0.
# Eliminadas langgraph-api y langgraph-cli (no necesarias aquí).

# ---------------------------------------------------------------------
# Frameworks Web
# ---------------------------------------------------------------------
fastapi>=0.115.0
uvicorn[standard]>=0.30.1
python-multipart>=0.0.9

# ---------------------------------------------------------------------
# Core Agéntico (LangChain / LangGraph / OpenAI)
# ---------------------------------------------------------------------
langchain>=0.2.6,<0.3.0
langgraph>=0.2.56,<0.3.0
langchain-openai>=0.1.9
openai>=1.50.0
langchain-community>=0.2.6

# ---------------------------------------------------------------------
# Persistencia y Base de Datos
# ---------------------------------------------------------------------
psycopg2-binary>=2.9.9
psycopg[binary]>=3.1.0
sqlalchemy>=2.0.31
asyncpg>=0.29.0

# ---------------------------------------------------------------------
# Utilidades
# ---------------------------------------------------------------------
requests>=2.32.3
python-dotenv>=1.0.1
pydantic>=2.7.4
httpx>=0.27.2
psutil>=5.9.8
google-auth>=2.30.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.198.0

# ---------------------------------------------------------------------
# Dependencias de checkpointing (para LangGraph)
# ---------------------------------------------------------------------
langgraph-checkpoint>=1.0.0
langgraph-checkpoint-postgres>=3.0.0
langchain-core>=0.2.0
